import { MydirDirectiveDirective } from './mydir-directive.directive';

describe('MydirDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MydirDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
